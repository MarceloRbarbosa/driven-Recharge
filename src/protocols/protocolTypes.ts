export type PhonesData = {
    phone_number: string,
    carrier_id: number,
    user_id: number,
    description: string
};

export type NewPhoneData = {
    id: number,
    phone_number: string,
    carrier_id: number,
    user_id: number,
    description: string
}
